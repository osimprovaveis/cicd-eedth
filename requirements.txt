# =============================================================================
# requirements.txt — dependências de produção
# =============================================================================
# Usado por:
#   - Dockerfile (build da imagem)
#   - pip-audit (gate de segurança do ci.yml)
#   - app.py (runtime)
# =============================================================================
flask==3.1.3
flask-sqlalchemy==3.1.1
requests==2.33.0
gunicorn==23.0.0
kubernetes==31.0.0
